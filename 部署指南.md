# 部署指南

> 本指南帮助您将资料库部署到 GitHub Pages，实现手机随时访问

---

## 准备工作

### 1. 创建 GitHub 账号

访问 [GitHub](https://github.com) 注册账号

### 2. 安装 Git

**Windows**：下载 [Git for Windows](https://gitforwindows.org/)

**Mac**：终端运行 `brew install git`

**Linux**：终端运行 `sudo apt install git` 或 `sudo yum install git`

### 3. 安装 Python（用于 MkDocs）

**Windows/Mac**：
1. 下载 [Python](https://www.python.org/downloads/)
2. 安装时勾选 "Add Python to PATH"

**验证安装**：
```bash
python --version
pip --version
```

---

## 第一步：创建 GitHub 仓库

### 1. 登录 GitHub

访问 https://github.com 登录您的账号

### 2. 创建新仓库

1. 点击右上角 **"+"** → **"New repository"**
2. 填写信息：
   - **Repository name**: `dandao-docs`（或其他名称）
   - **Description**: 丹道修炼资料库
   - **Public**: 选择（Private也行，但Pages需要付费）
   - **不要勾选** "Add a README file"
3. 点击 **"Create repository"**

### 3. 记录仓库地址

创建成功后，页面显示的地址如：
```
https://github.com/yourusername/dandao-docs
```

---

## 第二步：克隆仓库到本地

### 1. 复制仓库地址

在仓库页面点击 **"Code"** → 复制 HTTPS 地址：
```
https://github.com/yourusername/dandao-docs.git
```

### 2. 克隆到本地

```bash
# 进入您想存放项目的目录
cd ~/Documents

# 克隆仓库
git clone https://github.com/yourusername/dandao-docs.git

# 进入项目目录
cd dandao-docs
```

---

## 第三步：上传项目文件

### 1. 将文件复制到仓库目录

将 `/workspace/丹道修炼资料库` 下的所有内容复制到克隆的仓库目录：
- `docs/` 文件夹
- `mkdocs.yml` 文件
- 其他配置文件

### 2. 查看文件

```bash
ls -la
```

应该看到：
```
docs/
mkdocs.yml
README.md
```

---

## 第四步：安装 MkDocs

```bash
# 安装 MkDocs 和 Material 主题
pip install mkdocs mkdocs-material

# 验证安装
mkdocs --version
```

---

## 第五步：本地预览

```bash
# 进入项目目录
cd dandao-docs

# 本地预览（http://127.0.0.1:8000）
mkdocs serve
```

打开浏览器访问 http://127.0.0.1:8000 查看效果

---

## 第六步：部署到 GitHub Pages

### 方法一：使用 mkdocs gh-deploy（推荐）

```bash
# 部署到 GitHub Pages
mkdocs gh-deploy

# 会提示输入 GitHub 用户名和密码（或 Personal Access Token）
```

### 方法二：手动部署

```bash
# 1. 在项目根目录初始化 Git（如果还没有）
git init

# 2. 添加所有文件
git add .

# 3. 提交
git commit -m "Initial commit"

# 4. 添加远程仓库
git remote add origin https://github.com/yourusername/dandao-docs.git

# 5. 推送
git push -u origin main

# 6. 生成静态网站
mkdocs build

# 7. 上传 site/ 目录到 GitHub Pages
mkdocs gh-deploy
```

---

## 第七步：启用 GitHub Pages

### 1. 进入仓库设置

在 GitHub 仓库页面 → **Settings** → **Pages**

### 2. 配置 Source

- **Source**: Deploy from a branch
- **Branch**: `gh-pages` / `main`（根据 mkdocs gh-deploy 的输出）
- **Folder**: `/ (root)`

### 3. 等待部署

几分钟后，网站地址变为：
```
https://yourusername.github.io/dandao-docs
```

---

## 常见问题

### Q1: 部署后页面空白？

检查 `mkdocs.yml` 中的 `site_url` 是否正确：
```yaml
site_url: https://yourusername.github.io/dandao-docs
```

### Q2: 中文搜索不工作？

确保 `mkdocs.yml` 中配置了中文搜索：
```yaml
plugins:
  - search:
      lang: zh
```

### Q3: 想用自定义域名？

在仓库根目录添加 `CNAME` 文件，写入您的域名

### Q4: 忘记了 GitHub 密码？

使用 Personal Access Token 代替密码：
1. GitHub → Settings → Developer settings → Personal access tokens
2. Generate new token（勾选 repo）
3. 使用 token 作为密码

---

## 后续更新

修改文档后，只需：

```bash
# 进入项目目录
cd dandao-docs

# 拉取最新（如果有其他人协作）
git pull

# 本地预览
mkdocs serve

# 确认无误后部署
mkdocs gh-deploy
```

详细更新流程见 [更新指南](./更新指南.md)

---

## 相关链接

- [MkDocs 文档](https://www.mkdocs.org/)
- [Material 主题](https://squidfunk.github.io/mkdocs-material/)
- [GitHub Pages 文档](https://docs.github.com/en/pages)
