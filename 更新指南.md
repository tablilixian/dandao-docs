# 更新指南

> 本文档说明如何更新网站内容

---

## 更新流程概览

```
修改文档 → 本地预览 → 提交更改 → 部署上线
```

---

## 详细步骤

### 第一步：进入项目目录

```bash
cd ~/Documents/dandao-docs  # 根据您的实际路径调整
```

### 第二步：修改文档

#### 方式一：直接编辑 Markdown 文件

1. 找到要修改的文件（在 `docs/` 目录下）
2. 用文本编辑器打开（VSCode、Typora、记事本等）
3. 修改内容
4. 保存

#### 方式二：添加新文档

1. 在 `docs/` 下创建新文件，如 `docs/新文章.md`
2. 编辑内容
3. 在 `mkdocs.yml` 的 `nav` 部分添加导航

```yaml
nav:
  - 首页: index.md
  - 个人修炼:
    - 个人化修炼方案: 个人修炼/个人化修炼方案.md
    - 新文章: 新文章.md  # ← 添加这里
```

### 第三步：本地预览

```bash
mkdocs serve
```

打开 http://127.0.0.1:8000 查看修改效果

### 第四步：提交更改到 Git

```bash
# 查看修改的文件
git status

# 添加所有修改
git add .

# 提交（写清楚修改内容）
git commit -m "更新了修炼方案，添加了新的心得"

# 推送到 GitHub
git push
```

### 第五步：部署到网站

```bash
mkdocs gh-deploy
```

等待几分钟后，访问 `https://yourusername.github.io/dandao-docs` 查看更新

---

## 常用更新场景

### 场景一：修改修炼日记

```bash
cd ~/Documents/dandao-docs

# 编辑日记
# 使用 VSCode 或其他编辑器打开 docs/个人修炼/修炼日记.md

# 预览
mkdocs serve

# 提交
git add docs/个人修炼/修炼日记.md
git commit -m "更新修炼日记 - 2026年5月13日"
git push

# 部署
mkdocs gh-deploy
```

### 场景二：添加新的修炼心得

```bash
# 创建新文件
touch docs/个人修炼/修炼心得_第五个月.md

# 编辑内容
# ...

# 更新导航（mkdocs.yml）
# 在 nav → 个人修炼 下添加新条目

# 提交并部署
git add .
git commit -m "添加第五个月修炼心得"
git push
mkdocs gh-deploy
```

### 场景三：批量更新多个文件

```bash
# 修改多个文件后

# 查看修改了哪些文件
git status

# 添加所有修改
git add .

# 提交
git commit -m "批量更新：完善太乙金华宗旨注释"

# 推送
git push

# 部署
mkdocs gh-deploy
```

---

## 快速更新脚本

创建一个快捷脚本，方便日常更新：

### Windows (update.bat)

```batch
@echo off
cd /d C:\Users\YourName\Documents\dandao-docs

echo 正在部署...

git add .
git commit -m "更新内容 - %date%"
git push

mkdocs gh-deploy

echo 部署完成！
pause
```

### Mac/Linux (update.sh)

```bash
#!/bin/bash
cd ~/Documents/dandao-docs

echo "正在部署..."

git add .
git commit -m "更新内容 - $(date)"
git push

mkdocs gh-deploy

echo "部署完成！"
```

使用方法：
```bash
# 给脚本执行权限
chmod +x update.sh

# 运行
./update.sh
```

---

## 版本管理建议

### 提交信息规范

| 类型 | 示例 |
|------|------|
| 添加内容 | `添加第三章回光之功详解` |
| 修改内容 | `修改修炼方案中的时间安排` |
| 修正错误 | `修正太乙金华宗旨中的错别字` |
| 更新日记 | `更新修炼日记 - 2026年5月` |

### 定期备份

```bash
# 每周备份一次
git tag backup-$(date +%Y%m%d)
git push origin backup-$(date +%Y%m%d)
```

---

## 多人协作（可选）

如果多人共同维护：

```bash
# 先拉取最新
git pull

# 修改...

# 提交
git add .
git commit -m "修改内容"
git push

# 如果有冲突，先解决再推送
git pull --rebase
git push
```

---

## 回滚到历史版本

如果更新出错，可以回滚：

```bash
# 查看历史提交
git log --oneline

# 回滚到某个版本（如 abc1234）
git reset --hard abc1234

# 强制推送（谨慎使用）
git push -f

# 重新部署
mkdocs gh-deploy
```

---

## 移动端更新（高级）

如果需要在手机上更新：

1. 使用 GitHub App 或手机浏览器编辑
2. 或使用 Termux（Android）等终端工具
3. 推荐使用电脑更新，手机查看

---

## 总结

最简单的更新流程：

```bash
# 1. 修改文件
# 2. 本地预览（可选）
mkdocs serve

# 3. 提交并部署
git add .
git commit -m "更新内容"
git push
mkdocs gh-deploy
```

每次更新约需 **1-2分钟**，网站 **1-2分钟后** 自动更新。
